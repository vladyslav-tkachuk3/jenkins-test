pipeline {
  agent any
  
  parameters {
    choice choices: ['dev', 'qa'], name: 'deploy_environment'
  }

  stages {
    stage('build') {
      steps {
        sh "echo ${params.deploy_environment}"
      }
    }
  }
}
