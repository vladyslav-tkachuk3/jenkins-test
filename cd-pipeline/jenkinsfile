pipeline {
  agent any
  
  stages {
    stage('build') {
      environment {
        env_name = sh(returnStdout: true, script: 'echo sdfsdf').trim()
      }
      steps {
        //sh 'env'
        sh 'echo $env_name'
      }
    }
  }
}
